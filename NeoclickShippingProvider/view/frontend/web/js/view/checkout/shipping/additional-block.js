define([
    'uiComponent'

], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            template: 'IIA_NeoclickShippingProvider/checkout/shipping/additional-block'
        }
    });
});

